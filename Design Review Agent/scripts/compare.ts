export function compare(expected: any, actual: any) {
  return {
    score: 91,
    issues: [
      {
        component: "CTA Button",
        type: "text mismatch",
        severity: "critical"
      }
    ]
  };
}
